import { PerfilView } from "@/modules/auth/components/perfil-view";

export const metadata = {
  title: "Mi Perfil | Sistema Médico",
  description: "Perfil de usuario y gestión de cuenta",
};

export default function PerfilPage() {
  return <PerfilView />;
}

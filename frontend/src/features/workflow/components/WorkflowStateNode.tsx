/**
 * @deprecated Prefer imports from `./bpmn/bpmnRegistry`.
 * Se mantiene para no romper imports existentes.
 */
export { BpmnTaskNode as WorkflowStateNode, workflowFlowNodeTypes } from './bpmn/bpmnRegistry'
